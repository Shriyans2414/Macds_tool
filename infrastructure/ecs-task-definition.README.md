# ECS Task Definition

Before deploying, replace YOUR_AWS_ACCOUNT_ID with your
actual AWS account ID in ecs-task-definition.json.

You can find your account ID by running:
  aws sts get-caller-identity --query Account --output text
